# which benchmarks to run
benchmarks = '../../benchmarks/**/*.bril'
# benchmarks = '../test/tdce/*.bril'
# how to extract the performance metric from stderr
extract = 'total_dyn_inst: (\d+)'

[runs.baseline]
pipeline = [
  "bril2json",
  "brili -p {args}",
]

[runs.global_const_fold]
pipeline = [
  "bril2json",
  "python3 global_const_fold.py",
  "brili -p {args}",
]
